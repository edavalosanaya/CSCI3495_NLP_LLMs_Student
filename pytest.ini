# Anchors pytest's rootdir at the repository root.
#
# Labs are run from inside their own folder (`pytest -k step1 -q`), and without
# this file pytest would treat that folder as the rootdir and never load
# weeks/conftest.py, so an unwritten step would fail with a traceback instead
# of reporting "not written yet".
[pytest]
testpaths = weeks homeworks
